{
  "name": "padelnearmeos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo 'Starting frontend' && cd frontend && npm start && echo 'Frontend started' && cd ../ && echo 'Starting backend' && cd backend && npm start && echo 'Backend started'"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.4"
  }
}
